TODO

i) more features (configurations)
ii) rename config to config.json
iii) change TODO structure (this thing) so that notes are placed next to code correlating to the item
iv) domain > config(.json)
v) split functions into separate files (e.g one file for RPC, one file for communications, etc)
vi) discord_command > config(.json)